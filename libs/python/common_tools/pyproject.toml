[project]
name = "common_tools"
version = "0.0.0"
description = "Common tools."
readme = "README.md"
dependencies = [
    "punq>=0.7.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
