[project]
name = "market"
version = "0.0.0"
description = "Project market. The online store"
readme = "README.md"
dependencies = [
    "starlette",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
