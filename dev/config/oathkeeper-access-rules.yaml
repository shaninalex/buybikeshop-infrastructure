- id: "backend:public"
  upstream:
    preserve_host: true
    url: "http://localhost:8082"
    # strip_path: "/api/auth"
  match:
    url: "http://localhost:8080/<{hooks/*,authenticate/*}>"
    methods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
  authenticators:
    - handler: noop
  authorizer:
    handler: allow
  mutators:
    - handler: noop

- id: "backend:private"
  upstream:
    preserve_host: true
    url: "http://localhost:8082"
    # strip_path: "/api/auth"
  match:
    url: "http://localhost:8080/api/v1/<**>"
    methods:
      - GET
      - POST
      - DELETE
      - PATCH
  authenticators:
    - handler: cookie_session
  mutators:
    # - handler: noop
    - handler: header
  authorizer:
    handler: allow

# https://www.ory.sh/docs/oathkeeper/guides/proxy-websockets
- id: "api:websocket:private"
  upstream:
    preserve_host: true
    url: "http://localhost:8082"
  match:
    url: "http://localhost:8080/ws"
    methods:
      - GET
      - POST
  authenticators:
    - handler: cookie_session
  mutators:
    - handler: header
  authorizer:
    handler: allow
  errors:
    - handler: json

- id: "assets"
  upstream:
    preserve_host: true
    url: "http://localhost:8082"
    # strip_path: "/api/auth"
  match:
    url: "http://localhost:8080/assets/<**>"
    methods:
      - GET
  authenticators:
    - handler: noop
  authorizer:
    handler: allow
  mutators:
    - handler: noop
